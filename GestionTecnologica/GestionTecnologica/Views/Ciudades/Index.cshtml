@model IEnumerable<GestionTecnologica.Models.Ciudad>

@{
    ViewData["Title"] = "Index";
}

<h2>Index</h2>

<p>
    <a asp-action="Create">Create New</a>
</p>
<table class="table">
    <thead>
        <tr>
                <th>
                    @Html.DisplayNameFor(model => model.Nombre)
                </th>
                <th>
                    @Html.DisplayNameFor(model => model.IdDepartamento)
                </th>
            <th></th>
        </tr>
    </thead>
    <tbody>
@foreach (var item in Model) {
        <tr>
            <td>
                @Html.DisplayFor(modelItem => item.Nombre)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.IdDepartamento)
            </td>
            <td>
                <a asp-action="Edit" asp-route-id="@item.IdCiudad">Edit</a> |
                <a asp-action="Details" asp-route-id="@item.IdCiudad">Details</a> |
                <a asp-action="Delete" asp-route-id="@item.IdCiudad">Delete</a>
            </td>
        </tr>
}
    </tbody>
</table>
